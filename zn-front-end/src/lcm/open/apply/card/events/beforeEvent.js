/**
 * 卡片编辑前事件
 * @param {String} moduleId - 主表区域编码
 * @param {String} key - 字段key值
 * @param {Object} value - 字段值
 * @param {Object} data - 主表字段数据集合
 */
export function beforeEvent(moduleId, key, value, data) {

}
